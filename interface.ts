let drawPoint = (point: { x:number, y:number}) => {
//Inline 
}

drawPoint({
    x:1 ,
    y: 2
})

//Like Java interface
interface Point{
    x: number,
    y: number
}

let drawinter = (point: Point) => {
    //Name of interface has to be capital
}


//Interfaces for integrations